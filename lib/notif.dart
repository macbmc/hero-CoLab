// ignore_for_file: prefer_const_constructors
import 'package:flutter/material.dart';
class NotifP extends StatelessWidget {
  const NotifP({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.black,
      ),
      body: NotifPP(),
    );
  }
}
class NotifPP extends StatelessWidget {
  const NotifPP({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
